[project]
name = "comfyui-sageattn-ada"
description = "SageAttention kernels for consumer video DiTs on Ada (RTX 40xx / sm89). MiniMax H3 self-attention today."
version = "0.1.0"
requires-python = ">=3.10"

[tool.comfy]
DisplayName = "SageAttention Ada"
PublisherId = "fbliss"
